import SupportView from "@/app/views/support";
import { metadata as supportMetadata } from "./metadata";

export const metadata = supportMetadata;

export default function SupportPage() {
  return <SupportView />;
}
